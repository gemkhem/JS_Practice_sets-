let age = prompt("Enter your age")
    if(age<=18){
        console.log("your age is below 18 so you can't drive")
    }
    else if(age>=60)
        console.log("your age is between 18 to 60,you can drive")
    else(age>60)
        console.log("your age is greater than 60 so you can't drive")